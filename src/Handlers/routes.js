// @flow
import LoginRoutes from './Login';
import SignupRoutes from './Signup';
import UploaderRoutes from './Uploader';

const routes = [].concat(LoginRoutes, SignupRoutes, UploaderRoutes);

export default routes;
